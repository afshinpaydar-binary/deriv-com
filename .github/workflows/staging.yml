name: Staging Environment

on: [push]

jobs:
  git-checkout-from-cache:
    runs-on: ubuntu-18.04
    steps:
      - name: test
        run: |
          ls -lha
          pwd
      - name: check version
        run: |
          node -v
          npm -v